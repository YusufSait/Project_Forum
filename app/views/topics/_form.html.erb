
<%= form_for [ @topic, @sub_topic ] do |f| %>
  <% if @sub_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @sub_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
